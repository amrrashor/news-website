export interface cardProps {
    author: string,
    title: string,
    description: string,
    url: string,
    urlToImage:string
    publishedAt: string,
    content:string,
}

export interface SectionHeaderProp {
    text:string
}